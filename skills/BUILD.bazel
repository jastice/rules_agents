package(default_visibility = ["//visibility:public"])

load("//rules_agents:defs.bzl", "agent_skill")

agent_skill(
    name = "rules_agents",
    root = "rules_agents",
    srcs = glob(["rules_agents/**"], exclude_directories = 1),
)
