package(default_visibility = ["//visibility:public"])

load("//rules_agents:defs.bzl", "agent_skill")

agent_skill(
    name = "missing_skill",
    root = "missing_skill",
    srcs = glob(["missing_skill/**"], exclude_directories = 1),
    tags = ["manual"],
)
